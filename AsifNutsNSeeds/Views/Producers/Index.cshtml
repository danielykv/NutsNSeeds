@model IEnumerable<Producer>

@{
	ViewData["Title"] = "List of Producers";
}


<div class="row">

	<div class="col-md-8 offset-md-2">

		<table class="table">
			<thead>
				<tr>
					<th>@Html.DisplayNameFor(model => model.ProfilePictureURL) </th>
					<th>@Html.DisplayNameFor(model => model.ProducerName) </th>
					<th>@Html.DisplayNameFor(model => model.ProducerBio) </th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var item in Model)
				{
					<tr>

						<td class="align-middle">
							<img class="rounded-circle" src="@item.ProfilePictureURL" alt="@item.ProducerName" style="max-width: 150px" />
						</td>

						<td class="align-middle">
							@Html.DisplayFor(modelItem => item.ProducerName)
						</td>
						<td class="align-middle">
							@Html.DisplayFor(modelItem => item.ProducerBio)

						</td>
						<td class="align-middle">
							<a class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i>Edit</a>
							<a class="btn btn-outline-info"><i class="bi bi-eye"></i>Details</a>
							<a class="btn btn-danger"><i class="bi bi-trash"></i>Trash</a>
						</td>

					</tr>
				}

			</tbody>


		</table>




	</div>





</div>